# lower bound :-
```
returns iterator of just greater than or equal to element

int start = lower_bound(begin(nums), end(nums), target) - begin(nums);     // index of target element

```

# Upper bound :-
```
returns iterator of just greater than the element

int start = upper_bound(begin(nums), end(nums), target) - begin(nums);     // index of target element

```
